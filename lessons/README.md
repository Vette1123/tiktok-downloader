# Lessons

One file per finished unit of work — a feature, a fix, a release, an audit, a
refactor. Named `YYYY-MM-DD-slug.md`, committed *with* the work rather than
after it.

Format:

```markdown
# <title>

## What
One paragraph: what the unit of work actually was.

## Mistakes
The wrong turns. What was assumed and turned out false, what was verified the
wrong way, what nearly shipped, what was built and then deleted. This section is
the point of the file — git already records what was built. If there genuinely
was no wrong turn, say so in one line and keep the file short.

## What worked
The approaches worth repeating.

## Rules
Short imperatives for next time.
```

Read the lessons touching an area before starting work in it; the table below is
the index for exactly that.

| Date | Lesson | The one thing |
| --- | --- | --- |
| 2026-08-10 | [Creem payout-account rejection](2026-08-10-creem-payout-rejection.md) | A reviewer reads the marketing, not the code — "no watermark" describes a circumvention tool even when the code only fetches public files. |
| 2026-08-10 | [Withdrawing the subscription](2026-08-10-withdrawing-the-subscription.md) | When the question is "who gets this feature", change the input to the decision, never the decision points — and a capability that must never be sold needs its own name, not a flag inside one that is. |
| 2026-08-13 | [Every generic platform was dead, and one was pretending not to be](2026-08-13-platform-sweep.md) | The shared public resolver is blocked by the origins it resolves, so each platform now reads its own embed surface first — and a sweep that only checks `success: true` will score an HTML error page as a working video. |
| 2026-08-13 | [The Instagram session was fine; every path that used it was dead](2026-08-13-instagram-media-api.md) | Instagram's GraphQL `doc_id` now answers "execution error" for everyone, so the credentialed path had to move to `/api/v1/media/<id>/info/` — prove a credential with one endpoint only it can answer before blaming it. |
| 2026-08-14 | [The server was never the slow half](2026-08-14-client-hedge-and-facebook-shapes.md) | A TikTok paste calls tikwm from the browser before it ever calls us, so a server-side measurement missed 6s of the wait — hedge a third party on the critical path, never await it on a long timeout. |
| 2026-08-14 | [The slow platform was not slow, and the broken one was never reached](2026-08-14-tiktok-latency-facebook-share.md) | A latency report is answered with a stopwatch, not a diff — TikTok's first upstream had started queueing (race them, don't reorder), and Facebook's own share links only redirect for a crawler. |
| 2026-08-15 | [The membership that could not be delivered](2026-08-15-buymeacoffee-webhook.md) | Support arrives before the account does, so a webhook that writes `users.grants WHERE email = ?` matches zero rows and calls it success — record support against the address, apply it at sign-in. |
| 2026-08-15 | [Two bad samples and I declared Instagram dead](2026-08-15-instagram-logged-out-wall.md) | Instagram answers deleted, restricted and never-existed with the same "may be broken" shell, so a platform-is-dead conclusion needs a post proven public by a third party — and the credentialed path is one real account, which hundreds of probe requests got locked with `checkpoint_required`. |
| 2026-08-10 | [Footer apps menu, batch textarea](2026-08-10-footer-apps-menu.md) | `.surface` is unlayered CSS that owns `position` — anything absolute against it needs a wrapper — and a layout change is not verified until the page is on screen. |
| 2026-08-15 | [The identifier every account hangs on is scoped to a project](2026-08-15-google-oauth-project-move.md) | A Google `sub` names a person within one Cloud project, so moving projects re-keys every account — and `users.email` has no UNIQUE constraint, so the orphan inserts without a single error. |
| 2026-08-15 | [The homepage was public, and Google still called it a login wall](2026-08-15-google-oauth-brand-verification.md) | A page proven public from one IP is proven public from one IP — and a Cloudflare setting looks like it changed nothing until the cache is purged, because the edge bakes its injected script into the stored HTML. |
| 2026-08-15 | [The bot protection that could not be told who the good bots were](2026-08-15-waf-crawlers-bot-fight-mode.md) | Free-plan Bot Fight Mode runs outside the Ruleset Engine, so no `skip` rule reaches it — allowlisting crawlers means turning it off and re-expressing the defence as WAF rules, then re-testing with a browser UA so the probe isn't answering about itself. |
| 2026-08-19 | [A stale directory answered a question I never asked it](2026-08-19-bmc-widget-static-export.md) | The grep that found nothing was pointed at a three-day-old `out/`, and fixing that step left its false conclusion — "React drops the tag" — standing through a build, a deploy and a commit message; when the evidence is void, re-run the conclusion. |
| 2026-08-20 | [The close button we broke ourselves](2026-08-20-bmc-widget-close-zindex.md) | The tip jar's chevron is a picture, not a button — the vendor closes via an overlay stacked above it, which our z-index band flattened; and the fix that restored the desktop left the phone with nothing to tap, because the real mobile close was sitting under the PWA status bar the whole time. |
| 2026-08-24 | [Universal downloads and the MP3 fallback that doubled its own wait](2026-08-24-universal-downloads.md) | A fallback must not re-run the round-trip that just failed (pass `skipCobalt` down), and tests passing is not deploys clean - run cf:build plus a wrangler-dev smoke when API code changes. |
| 2026-08-24 | [Supporter extras night: playlists, subtitles, universal page](2026-08-24-supporter-extras.md) | Fixtures fail before code does; pure modules make Worker handlers trustworthy; and consolidation means deleting the old copy, or the build says so. |
| 2026-08-25 | [The review that found the copy lying, not the code](2026-08-25-pre-push-review.md) | Every gate was green before the review started, so everything it found was invisible to tooling — the FAQ promised a free batch queue that is Pro-only, a comparator test named "prefers mp4" passed for a different reason than its name, and `cf:startup` read a stale directory to report a bundle 68 KiB smaller than the one that would have failed CI. |
| 2026-08-29 | [CPU startup, SEO titles, marketing](2026-08-29-cpu-startup-seo-titles-marketing.md) | Startup was the whole cpuTimeP50 and an import cycle was a third of it, while types, lint, tests and the byte budget all stayed green. |
| 2026-08-29 | [The 504s that were never real](2026-08-29-the-504s-that-were-never-real.md) | Every 504 on this zone was Cloudflare asking its own cache; only eyeball rows are people, and reporting a status code without its requestSource put two non-defects in a hand-off. |
| 2026-08-29 | [The Product Hunt draft, and the CPU floor that would not move](2026-08-29-product-hunt-draft-and-the-cpu-floor.md) | Two probes of a do-nothing route split 0.29 ms from 7.18 ms — the cost is isolate creation, not our bundle, so the local 5.29 → 3.24 ms never reached production; and long text typed into a browser field lost eleven characters in silence. |
| 2026-08-29 | [Directories, a Show HN, and the filter that ate the Reddit posts](2026-08-29-directories-show-hn-and-the-spam-filter.md) | Both Reddit posts read back `removed_by_category: "reddit"` — a seven-year-old account with 5 karma cannot post links, and the page looks perfectly normal to its author; meanwhile a 403 Cloudflare challenge was mistaken for a broken icon route, and a form field that looked filled had never reached the model. |
| 2026-08-31 | [Dated releases on deploy, and the audit that followed](2026-08-31-dated-releases-on-deploy.md) | Two of three changes copied from the sibling repo had no call site here; then the health report's 52-character cut hid `HeadlessChrome` behind the boilerplate every browser UA opens with, making four rows of correctly-challenged scrapers look like blocked users. |
| 2026-08-31 | [Nothing was watching the platforms](2026-08-31-nothing-was-watching-the-platforms.md) | Every test asserts against a fixture, so the suite stays green after the site it was captured from changes — only a live resolve proves a platform works, and the monitor that does it needed six list entries and a cron, not the Worker cron and D1 table designed first. |
| 2026-08-31 | [Subrequests are the CPU, and the smoke test was pointed at the wrong host](2026-08-31-subrequests-are-the-cpu.md) | A failing resolve cost 9 ms of CPU in production and 0.9 ms locally with stubbed fetches — the cost was four outbound HTTPS calls at ~2 ms each, three of them to public cobalt instances that answer 400 for hosts they do not serve; gating those took the same request to 1 subrequest and 1 ms. |
| 2026-09-06 | [The third copy of the Play-only list](2026-09-06-the-third-copy-of-the-play-only-list.md) | Every companion-app card here sent iPhone visitors to Google Play for two days after both apps shipped on iOS — the same defect already fixed in Reely, in a near-copy of the same module. And a brand colour is a claim just like a brand icon. |
| 2026-09-06 | [The tunnel that served a JPEG](2026-09-06-the-tunnel-that-served-a-jpeg.md) | A reel downloaded as an image because a Cobalt tunnel answered `status: "tunnel"` with the post's cover and every guard we had asked only "does it serve bytes" — the diagnosis was one `curl` of our own downloadUrl, and the monitor never noticed because no Instagram probe had ever asserted that a video was a video. |
| 2026-09-06 | [File names, and the order things arrive in](2026-09-06-file-names-and-the-order-things-arrive-in.md) | Supporters can shape their filenames now, and a minute-resolution timestamp was why downloads landed "in weird positions" — but gating the carousel ZIP to make one Pro claim true would have made nine public ones false, which is the 2026-08-25 finding wearing the other face. |
| 2026-09-06 | [The probe already knew the size](2026-09-06-the-probe-already-knew-the-size.md) | The file size was sitting in a `Content-Range` header the reachability probe was already throwing away, so the card gained it for zero subrequests — and grepping the Instagram carousel fix's shape found Twitter dropping every video after the first, a defect nobody had ever reported. |
| 2026-09-07 | [YouTube was answering with an embed, and the monitor said fine](2026-09-07-youtube-was-answering-with-an-embed.md) | Seven of eight YouTube links resolved to a viewable iframe and nothing downloadable, hidden because the one probe in the monitor is the one video that still works — ANDROID_VR is bot-blocked from Cloudflare addresses, no client publishes a muxed stream any more, and the audio and subtitles were sitting in a response the code was throwing away. |
| 2026-09-07 | [Two fewer taps](2026-09-07-two-fewer-taps.md) | Drop a link on the page, paste with nothing focused, or let a supporter's result save itself — and the near-miss was a `preventDefault` on every `dragover`, which would have swallowed dropped files and done nothing with them. |
| 2026-09-07 | [Copy, switch, saved](2026-09-07-copy-switch-saved.md) | Clipboard watch plus auto-save turns copying a link into the whole interaction — and shipping focus-return in the same batch as Escape-to-restart made each of them break the other, because the cursor lands in the one place the Escape handler is right to ignore. |
| 2026-09-07 | [The cache outlived the link](2026-09-07-the-cache-outlived-the-link.md) | A Cobalt tunnel lives 92 seconds and the cache held it for 180, so half of every cache window served a Download button that answered 404 — and separately, one rate-limited reel was cached and handed to the next seven callers as a JPEG. Both found by adding a TikTok probe, not by looking. |
| 2026-09-07 | [Have I already got this one?](2026-09-07-have-i-already-got-this-one.md) | The Recent list recorded resolves, not downloads, so it could never answer the one question it looked like it answered — and the first fix un-marked a file the moment you pasted its link again, which is exactly when the mark was about to be useful. |
| 2026-09-07 | [Told no by the product that says yes](2026-09-07-told-no-by-the-product-that-says-yes.md) | Pasting a YouTube playlist answered "Could not download this generic content" while the batch queue sat underneath waiting to expand exactly that — and reusing `detectImportSource` to spot it found that same function had been reading a Vimeo video and a Pinterest pin as collections for months. |
| 2026-09-07 | [The JPEG came back through another door](2026-09-07-the-jpeg-came-back-through-another-door.md) | `cobalt:stills(1)` in a give-up line shipped an hour earlier: yesterday's fix probes a candidate *stream*, and a gallery never claims to be one, so the reel's cover reached the visitor as `images[0]` — the original complaint reproduced exactly, by a route the guard was not standing at. |
| 2026-09-07 | [The pile you have not saved](2026-09-07-the-pile-you-have-not-saved.md) | Recording which Recent rows were downloaded turned the rest into a number, and the number is the offer: "2 not saved yet" plus the queue. The most persuasive place to describe a paid feature is where the need is already on screen, not on the pricing page. |
| 2026-09-07 | [Two knobs, one memory](2026-09-07-two-knobs-one-memory.md) | The download readout had bytes and a rate but not the one number anybody wants; and per-platform quality had been shipping alone since a format memory is the same shape, which YouTube losing video made worth having rather than merely symmetrical. |
| 2026-09-07 | [The other half of a download](2026-09-07-the-other-half-of-a-download.md) | A save on a phone is half the job, and the share sheet only opens inside a live gesture — so the bytes are held rather than re-fetched. The lint rule that fired on the saved-file state was describing a real cascading-render path, and the fix I nearly wrote for it came with a justification I had invented. |
