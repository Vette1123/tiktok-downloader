# Public base URL of the deployed site — used for canonical URLs,
# OpenGraph/Twitter metadata, sitemap entries and JSON-LD.
# Leave unset locally to fall back to the production URL.
NEXT_PUBLIC_SITE_URL=
